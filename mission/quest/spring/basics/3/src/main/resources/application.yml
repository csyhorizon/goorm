spring:
  h2:
    console:
      enabled: true  # H2 콘솔 활성화
      path: /h2-console  # 콘솔 경로 설정

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: goorm
    password: